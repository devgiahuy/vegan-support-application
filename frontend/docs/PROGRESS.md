# PROGRESS — Lịch sử & % hoàn thành theo task / chức năng

> File duy nhất để trả lời "xong bao nhiêu %". Agent BẮT BUỘC cập nhật sau mỗi task (xem `ARCHITECTURE.md` mục 7).
> Phạm vi MVP: xem `SRS_Vegan_Support_Application.md` PHỤ LỤC B.

## Cách tính % (thống nhất, tránh đoán mò)

Mỗi feature MVP = 100%, chia theo 7 bước scaffold (`ARCHITECTURE.md` mục 5):

| Bước                                                            | Trọng số |
| --------------------------------------------------------------- | -------- |
| UI route + components (loading/error/empty, tiếng Việt, shadcn) | 30%      |
| DTO + Model interfaces                                          | 10%      |
| Mapper (`pickField` + `safe*`, full field)                      | 20%      |
| API (`api-endpoints` + `api/*.api.ts`, không leak DTO)          | 15%      |
| Queries (Key Factory + hooks, invalidate)                       | 10%      |
| Form/schema + phân quyền/RBAC (nếu có)                          | 5%       |
| Verify (`tsc --noEmit`, `npm test`, `npm run build` + test tay) | 10%      |

- Chỉ tick % khi bước đó đã merge vào cây làm việc (không tính code nháp).
- Task docs (SRS/ARCHITECTURE) tính riêng: 100% = đã sửa + đã đọc lại file sau sửa.

## Bảng tổng (cập nhật sau mỗi task)

| #   | Task / Chức năng (MVP)                                                                                      | UC          | %    | Trạng thái | Ghi chú                                                                                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------------------------------------------------------------------------- | ----------- | ---- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Nền tảng: shell, providers, axios/auth-token/server-fetch, mapper core, api-endpoints, logo, hero animation | —           | 70%  | Đang làm   | Đã tích hợp Remotion 2D Food Animation 9 phase với ảnh thực tế tối ưu WebP cho Hero Section; logo & favicon; còn thiếu Cloudinary + search Postgres                                                                                                                                                                          |
| 1   | Auth UC-01 (login/register/Google, onboarding `dietSchool`)                                                 | UC-01       | 95%  | Đang làm   | API thật đã tích hợp (register/login/refresh/logout+`/users/me`, DTO/Model/Mapper/Query+test, form+RBAC); còn test tay VS-1..VS-8 (chờ BE local) + OTP/OAuth (Phase 2/P3)                                                                                                                                                    |
| 2   | Blog/Post UC-02 & Công thức món chay + ảnh Cloudinary (scaffold + fixture, chờ BE READY mới tính là tích hợp xong theo BACKEND_INTEGRATION §12) | UC-02 | 85% | Đang làm | Hoàn thiện trọn bộ 7 tầng scaffold theo spec 005-content-media + đợt fix review F1–F9 + đợt nối live-shape (params `category` đúng BE strict, DTO/Mapper đọc `revision`/`recipe`/`media`, upload signature + `uploadUrl` BE, create/update `body`/`media`/`categoryIds`/`expectedVersion`, AuthGuard/silent-refresh/logout-clear-cookies, uploader metadata): DTO/Model/Mapper + 9 post tests & 6 api-fallback tests (76/76 toàn dự án); API client + TanStack Query/Mutations; Zod schemas; UI routes; ImageUploader/VideoUploader; DeletePostDialog (expectedVersion). Đã verify live với BE `:4000`: `GET /posts?type=RECIPE&category=<uuid>` 200 (trước đó `categoryId` 400 đúng như user báo). BE chưa có dữ liệu content nên list live đang rỗng. |
| 3   | Comment/Vote UC-03                                                                                          | UC-03       | 70%  | Đang làm   | Scaffold live-shape 7 tầng `features/community` (DTO đủ 11 ops/Model/Mapper + 12 tests, fixtures, API fixture 0 request, Query, zod, 7 components); nâng cấp `comment-section` shared + gắn thread/summary/vote/rating/bookmark vào 3 detail + bookmarks list ở `/profile`; chờ BE READY mới nối live |
| 4   | Tìm kiếm UC-04 (Postgres, phân trang) + Catalog (danh mục/nguyên liệu)                                      | UC-04       | 75%  | Đang làm   | Catalog API đã tích hợp (`features/category`, `features/ingredient`, `features/admin-catalog`, `/categories`, admin tabs); filter `/recipes` (RECIPE_GROUP) + `/articles` (CONTENT_TOPIC) đã dùng cây thật (best-effort theo tên tới khi API recipe/post READY); còn test tay VC-1..VC-6; search Postgres bài viết vẫn thiếu |
| 5   | Upload video UC-05 (Cloudinary ≤100MB, YouTube embed) (scaffold + fixture, chờ BE READY mới tính là tích hợp xong theo BACKEND_INTEGRATION §12) | UC-05 | 80% | Đang làm | Hoàn thiện trọn bộ 7 tầng scaffold theo spec 005-content-media + đợt fix review F1–F9: DTO/Model/Mapper + 4 Vitest tests pass; API client + TanStack Query; VideoPlayer đa năng (YouTube + HTML5 + placeholder URL lỗi); VideoUploader (tải Cloudinary có tiến trình & dán URL YouTube) đã được nối vào `/videos/new` viết lại (AuthGuard + zod + mutation, xóa mock AI STT/500MB cũ); UI routes (`/videos`, `/videos/[id]`, `/videos/new`). Chờ BE API READY để nối live. |
| 6   | Menu rule-based UC-06 (nhập tay)                                                                            | UC-06       | 95%  | Đang làm   | API thật 5 endpoint meal-plans đã tích hợp (`features/meal-plan`, DTO/Model/Mapper + 14 tests, API, Query, zod Thứ Hai + goal, dialogs swap/delete, `/meal-plans` + `/meal-plans/saved` viết lại + `/meal-plans/[id]` mới); verify live BE `:4000` (generate 21/21, swap `lockVersion`, delete `{deleted:true}`); còn test tay trình duyệt MP-1..MP-6 |
| 7   | Chatbot UC-07 (LLM trực tiếp + disclaimer)                                                                  | UC-07       | 95%  | Đang làm   | API thật 5 endpoint chat private đã tích hợp (`features/chat`, DTO/Model/Mapper + parser SSE + 12 tests, API axios 4 ops + `chat-stream.ts` fetch POST, Query + buffer ephemeral, zod composer, 7 components, `/assistant` viết lại không AuthGuard); verify live BE `:4000` (guest cookie `chatGuest`, SSE đủ 5 events, history, feedback upsert); scaffold share/public/verify 3 ops (DTO suy luận/Mapper + 12 tests, fixtures, API fixture 0 request, 6 components, route `/assistant/public`); còn test tay trình duyệt AC-1..AC-6 + CS |
| 8   | Quán ăn UC-12 (Maps JS + Places, web)                                                                       | UC-12       | 70%  | Đang làm   | Scaffold 7 endpoint `features/restaurant` (DTO suy luận/Model/Mapper + 12 tests haversine, fixtures, API fixture 0 request + 0 maps, Query, zod, 10 components); viết lại `/restaurants` + `/restaurants/[id]` (vị trí/form địa chỉ, khung bản đồ CSS) + tab `restaurants` dashboard; giữ `PLANNED`/`FE integrated No`; còn test tay RT + BE READY |
| 9   | Sức khỏe UC-13 (nhập tay)                                                                                   | UC-13       | 90%  | Đang làm   | API HealthProfile + diet/preferences/schedule đã tích hợp (`features/profile`, `features/diet-preferences`, refactor `/profile`); avatar chuyển từ nhập URL sang upload Cloudinary + fix hiển thị header/banner (16/09/2026); còn test tay VP-1..VP-7 |
| 10  | Moderation UC-11 + Contributor UC-16/17 (spec 006, scaffold live-shape, chờ BE READY chính thức) | UC-11/16/17 | 90% | Đang làm | Hoàn thiện 7 tầng `features/review` (DTO/Model/Mapper + 5 tests, API, Query, zod reason, dialog, table); tạo `/admin/dashboard`, `/contributor/dashboard`, auto-redirect `/review`, đồng bộ menu dropdown và middleware RBAC; scaffold moderation-admin 6 ops `features/moderation` (DTO/Model/Mapper + 12 tests, fixtures, API fixture 0 request, 3 tabs dashboard) chờ BE READY; scaffold contributors 4 ops `features/contributor` (DTO/Model/Mapper + 12 tests, fixtures, API fixture 0 request, tab profile + tab dashboard) chờ BE READY |
| 11  | Docs: SRS v1.4 MVP, ARCHITECTURE mapper rule, PROGRESS + WORK-LOG                                           | —           | 100% | Xong       | Ngày 2026-09-15                                                                                                                                                                                                                                                                                                              |
| 12  | Recommendations (gợi ý home + consent + behavior events)                                                    | —           | 90%  | Đang làm   | API thật 4 capability đã tích hợp (`features/recommendation`, DTO/Model/Mapper + 12 tests, API, Query, hook `use-track-behavior-event`, khối home member + switch consent thật ở tab privacy, 4 điểm chạm event); verify live BE `:4000` (PUT off → cold-start + chặn event, restore on); xóa history vẫn PLANNED ngoài phạm vi; còn test tay trình duyệt RC-1..RC-5 |
| 13  | Trust-safety leftovers (báo cáo vi phạm + xóa lịch sử)                                                       | —           | 70%  | Đang làm   | Scaffold 2 endpoint `features/safety` (DTO đủ 2 ops/Model/Mapper + 12 tests, fixtures, API fixture 0 request, Query 2 hooks, zod, nút + 2 dialogs ở shared); gắn báo cáo vào 3 detail + bình luận (chặn tự báo cáo/trùng), thay toast giả xóa lịch sử ở tab privacy; giữ `PLANNED`/`FE integrated No`; còn test tay TS + BE READY |
| 14  | Notifications (chuông + danh sách + đã đọc)                                                                  | —           | 70%  | Đang làm   | Scaffold 3 endpoint `features/notification` (DTO suy luận/Model/Mapper + 12 tests timeAgo + capped, fixtures, API fixture 0 request, Query polling 60s + lạc quan rollback, chuông + panel + item ở header); giữ `PLANNED`/`FE integrated No`; còn test tay NT + BE READY |
| 15  | AI governance (giám sát AI cho admin)                                                                        | —           | 70%  | Đang làm   | Scaffold 5 endpoint `features/ai-governance` (DTO suy luận/Model/Mapper + 12 tests gồm redaction, fixtures, API fixture 0 request, Query 5 hooks, zod khoảng ngày + lý do, 5 components); tab `ai-governance` dashboard (tổng quan + log che mờ + cờ + công tắc); giữ `PLANNED`/`FE integrated No`; còn test tay AG + BE READY |

## Lịch sử cập nhật (mỗi dòng = 1 lần agent sửa %)

| Ngày (UTC) | Task                                                                 | % cũ → % mới                           | Lý do                                                                                                                                                                                                                                                                          |
| ---------- | -------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-15 | Khởi tạo file PROGRESS                                               | — → bảng trên                          | Seed lần đầu từ UI hiện có (mock); agent phải rà soát lại ở task kế tiếp                                                                                                                                                                                                       |
| 2026-09-15 | Auth UC-01 (spec 001-user-auth)                                      | 50% → 95%                              | Tích hợp API thật 5 endpoint (UI 30 + DTO/Model 10 + Mapper 20 + API 15 + Queries 10 + Form/RBAC 5 + Verify 5/10); còn test tay end-to-end                                                                                                                                     |
| 2026-09-15 | Sức khỏe UC-13 + diet (spec 002-user-profile)                        | 20% → 90%                              | Tích hợp 6 endpoint users/diet (UI 30 + DTO/Model 10 + Mapper 20 + API 15 + Queries 10 + Form 5 + Verify 0/10); còn test tay VP-1..VP-7                                                                                                                                        |
| 2026-09-15 | Catalog (spec 003-catalog)                                           | 30% → 75% (gộp vào task #4)            | Tích hợp 13 endpoint catalog (UI 30 + DTO/Model 10 + Mapper 15 + API 10 + Queries 5 + Form 5 + Verify 0/10); còn test tay VC-1..VC-6                                                                                                                                           |
| 2026-09-16 | Filter danh mục thật ở `/cong-thuc` + `/bai-viet`                    | 75% → 75% (task #4, giữ nguyên)        | Dùng lại `GET /categories` đã READY (không endpoint mới): `CategoryFilterList` (RECIPE_GROUP) + `CategoryFilterPills` (CONTENT_TOPIC), loading/error/empty đầy đủ; lọc mock best-effort theo tên                                                                               |
| 2026-09-16 | Gắn entry point + rule cấm trang mồ côi                              | 75% → 75% (task #4, giữ nguyên)        | Footer + link ngữ cảnh → `/danh-muc?type=`, dropdown admin theo role, rule mới trong ARCHITECTURE.md; `tsc`/`eslint`/`build` pass                                                                                                                                              |
| 2026-09-16 | URL test nhanh + ingredient search vào `/danh-muc`                   | 75% → 75% (task #4, giữ nguyên)        | Quickstart §2 bảng URL test; `/danh-muc` thêm `#tra-cuu`/`#phan-giai`, `/admin` nhận `?tab=`; `tsc`/`eslint`/`build` pass                                                                                                                                                      |
| 2026-09-16 | Fix auth 3 lỗi (REUSED, admin guard, redirect)                       | 75% → 75% (task #4 giữ) / Auth 95% giữ | `sharedRefresh` dedup + Web Locks, proxy login/register, `API_BASE_URL` via rewrites, `AuthGuard` hydrate, login admin → `/admin`; quickstart recovery; `tsc`/`eslint`/`build` pass                                                                                            |
| 2026-09-16 | Testing ingredient mapper (mở rộng)                                  | 75% → 75% (task #4, giữ nguyên)        | Mapper test 8 → 16 case (`toSingleModel`, envelope null, snake_case, alias biến thể, compat/warnings null); `tsc` sạch, `npm test` 66/66, `eslint` sạch; còn test tay VC-2..VC-6                                                                                               |
| 2026-09-16 | Xóa mock trang `/recipes`                                            | 75% → 75% (task #4, giữ nguyên)        | List + detail không còn `MOCK_RECIPES` (empty/404 trung thực vì BE chưa có API công thức); giữ filter danh mục thật; `tsc`/`test` 66/66/`eslint`/`build` pass                                                                                                                  |
| 2026-09-16 | Tích hợp bộ nhận diện logo VeggieConnect                             | 60% → 65% (task #0)                    | Cắt chuẩn bộ nhận diện từ ảnh người dùng cung cấp: logo-mark (512x512, favicon, apple-icon), logo-horizontal (light + dark), BrandLogo component, cập nhật header, footer, auth, admin, metadata; `tsc` sạch, `npm test` 66/66                                                 |
| 2026-09-16 | Rename route FE sang tiếng Anh (spec 004-fe-english-routes)          | % giữ nguyên mọi task                  | Đổi 20 slug (`/recipes`, `/articles`, `/videos`, `/categories`, `/restaurants`, `/meal-plans`, `/assistant`, `/search`, `/profile`, `/verify-otp`), 20 redirect 308 giữ query/hash, cập nhật toàn bộ link nội bộ + docs route; `tsc` sạch, `npm test` 66/66, `next build` pass |
| 2026-09-16 | Hero Section Remotion 2D Food Animation (spec update-UI-herosection) | 65% → 70% (task #0)                    | Hoàn thiện hoạt cảnh 9 phase bằng @remotion/player (240f @ 30fps), vector SVG art-directed (tô gốm, 5 nguyên liệu, Buddha Bowl hoàn chỉnh), floating badges đồng bộ pulse, hỗ trợ prefers-reduced-motion; `tsc` sạch, `npm test` 66/66, `next build` pass                      |
| 2026-09-16 | Tích hợp ảnh thực tế vào Hero Food Animation                         | 70% → 70% (task #0, giữ nguyên)        | Xử lý nén và cắt viền checkerboard bộ 7 ảnh thực tế của user bằng sharp sang WebP (giảm >97% từ 18MB xuống 412KB), đưa vào Remotion Composition với GPU translate3d 60 FPS, cập nhật skeleton & reduced-motion fallback; `tsc` sạch, `npm test` 66/66, `next build` pass       |
| 2026-09-16 | Content & Media (spec 005-content-media: Recipe, Post, Video)        | UC-02: 40% → 85% / UC-05: 30% → 80%   | Triển khai trọn vẹn 7 tầng scaffold cho 3 domain Recipe, Post/Article, Video: DTO/Model/Mapper + 12 Vitest tests pass; API clients + TanStack Query/Mutations (fallback mock fixtures); Zod schemas; ImageUploader (Cloudinary signature); VideoUploader & VideoPlayer kép (YouTube + HTML5); DeletePostDialog; các trang `/recipes/*`, `/articles/*`, `/videos/*`; `tsc` sạch 0 lỗi, `npm test` 64/64 pass, `next build` 26 routes pass |
| 2026-09-16 | Nối live-shape content + fix 3 lỗi testing (categoryId 400, silent-refresh, logout đăng video) | UC-02 giữ 85% / UC-05 giữ 80% (BE chưa đánh READY chính thức; list live rỗng vì chưa seed content) | Đổi query `categoryId`→`category` (xác nhận `category` 200 / `categoryId` 400 với BE `:4000`); DTO/Mapper/fixtures theo `postSchema` (`revision`/`recipe`/`media`); upload signature + `uploadUrl`; create/update `body`/`media`/`categoryIds`/`expectedVersion`; AuthProvider bỏ check `document.cookie` (HttpOnly) + logout clear cookies; uploader trả metadata; `tsc` sạch, `npm test` 76/76, `next build` 26 routes pass |
| 2026-09-16 | Xóa mock pages chống nhầm data thật/giả (home/search/restaurants/articles/profile + usePostStore + mock-*.ts + restaurant/product phantom) | % giữ nguyên (dọn mock, không thêm endpoint) | Home dùng `useRecipesQuery`; search gọi 3 queries thật (+`enabled` khi có từ khóa); restaurants empty trung thực; articles bỏ import store thừa; profile lọc bài theo user + `DeletePostDialog`; vote/lưu local-state; xóa `usePostStore`, 4 `mock-*.ts`, cả folder `restaurant`/`product` orphan; `tsc` sạch, `npm test` 87/87, `next build` 26 routes pass, eslint 0 errors |
| 2026-09-16 | Triển khai Content Review Queue (spec 006: T001–T020) | Task #10 (UC-11/16/17) 20% → 70% (scaffold live-shape; test tay + BE READY còn lại) | 7 tầng `features/review` (DTO/Model/Mapper + 5 tests, API strict 5 param, Query/mutations, zod reason, dialog, table, `/review` + tab `/admin` viết lại); `tsc` sạch, `npm test` 93/93, `next build` pass; `FE integrated` review-queue + changelog 2.5 (giữ `PLANNED`) |
| 2026-09-16 | Avatar upload + fix hiển thị header/banner `/profile` | Task #9 giữ 90% (không endpoint mới) | Bỏ input URL, thêm `AvatarUploader` (signature → Cloudinary → PATCH URL, chặn submit `blob:` mock); thêm `AvatarImage` vào header + banner; schema cho phép preview mock ở form; mapper test Cloudinary URL; `tsc` sạch, `npm test` 99/99, `next build` pass |
| 2026-09-16 | Route `/admin/dashboard`, `/contributor/dashboard` & đồng bộ điều hướng theo role | Task #10: 70% → 80% | Tạo `/admin/dashboard` (chuyển /admin tự động sang /admin/dashboard), đưa /review vào admin dashboard; tạo `/contributor/dashboard` (ReviewQueue + Lối tắt đóng góp + Tiêu chuẩn kiểm duyệt); auto-redirect `/review` theo role; cập nhật header dropdown và middleware; `tsc` sạch 0 lỗi, `npm test` 99/99 pass, `next build` 29 routes pass |
| 2026-09-16 | Tích hợp Meal Planner API thật (spec 007-meal-planner-integration, T001–T027) | Task #6 (UC-06) 30% → 95% (còn test tay trình duyệt MP-1..MP-6) | 7 tầng `features/meal-plan` (DTO/Model/Mapper + 14 tests, API 5 ops, Query Key Factory + 5 hooks, zod weekStart Thứ Hai, dialogs swap/delete version-gated, 7 components); viết lại `/meal-plans` + `/meal-plans/saved`, tạo `/meal-plans/[id]`; verify live BE `:4000` (generate 21/21, swap theo `lockVersion`, delete idempotent; fix shape thật `canonicalName/amount`, slot-level calories, `MICRONUTRIENT_DATA_PARTIAL`); `tsc` sạch, `npm test` 113/113, `eslint` sạch, `next build` 27 routes pass |
| 2026-09-17 | Redesign trải nghiệm Authentication (SaaS Scandinavian split 50/50 + Editorial Hero) | Task #1 giữ 95% (hoàn thiện UI/UX premium, bảo toàn 100% logic) | Thay mobile Remotion mockup bằng AuthEditorialHero (ảnh thực tế, 3 floating cards, GPU transform); nâng cấp AuthCard (bo góc 28px, segmented tabs, input 48px, button brand green); tích hợp AuthCompactBrand trên mobile; SafeImage cho phép null/undefined; browser preview desktop/mobile đã verify; tsc sạch, npm test 113/113, next build 29 routes pass |
| 2026-09-17 | Tích hợp Chat AI private (spec 008-ai-chat-integration, T001–T026) | Task #7 (UC-07) 20% → 95% (còn test tay trình duyệt AC-1..AC-6) | 7 tầng `features/chat` (DTO/Model/Mapper + parser SSE + 12 tests, API axios 4 ops + `chat-stream.ts` fetch POST tách interceptor, Query buffer ephemeral + commit ở `message_complete`, zod composer, 7 components); viết lại `/assistant` (thread + sidebar phiên + `?session=`, không AuthGuard); verify live BE `:4000` (guest cookie `chatGuest`, SSE đủ events, history, feedback upsert; fix label `resetAt` ISO → giờ Việt); `tsc` sạch, `npm test` 125/125, `eslint` sạch, `next build` pass |
| 2026-09-17 | Tích hợp Recommendations (spec 009-recommendations-integration, T001–T026) | Task #12 mới → 90% (còn test tay trình duyệt RC-1..RC-5) | `features/recommendation` (DTO/Model/Mapper + 12 tests, API 4 ops, Query + hook `use-track-behavior-event` ở `src/hooks/`, khối home member + switch consent thật thay mock privacy, 4 điểm chạm event); verify live BE `:4000` (home `behavioral-v1` + reasons thật, consent GET/PUT, PUT off → cold-start + `PERSONALIZATION_CONSENT_REQUIRED`, restore on); `tsc` sạch, `npm test` 137/137, `eslint` 0 errors, `next build` pass |
| 2026-09-17 | Scaffold Community live-shape + fixture (spec 010-community-integration, T001–T029) | Task #3 (UC-03) 20% → 70% (scaffold; test tay CM + BE READY còn lại) | `features/community` (DTO đủ 11 ops/Model/Mapper + 12 tests ép reply 1 tầng, fixtures bám contract, API fixture 0 request + kho bộ nhớ, Query 8 hooks, zod, 7 components); nâng cấp `comment-section` (xóa 404 dòng mock) + gắn thread/summary/vote/rating/bookmark vào 3 detail + bookmarks ở `/profile`; giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 151/151, `eslint` 0 errors, `next build` pass |
| 2026-09-17 | Scaffold Moderation-admin live-shape + fixture (spec 011-moderation-admin-integration, T001–T026) | Task #10 (UC-11/16/17) 80% → 85% (scaffold; test tay MA + BE READY còn lại) | `features/moderation` (DTO đủ 6 ops/Model/Mapper + 12 tests, fixtures, API fixture 0 request, Query 6 hooks map sẵn conflict/self/protected, zod reason bắt buộc, 6 components); 3 tabs mới dashboard (không đụng tab users sẵn có, giữ RBAC/`?tab=`); giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 163/163, `eslint` scope 0 errors (lỗi dashboard có sẵn ngoài scope), `next build` pass |
| 2026-09-17 | Scaffold Contributors live-shape + fixture (spec 012-contributor-applications, T001–T025) | Task #10 (UC-11/16/17) 85% → 90% (scaffold; test tay CA + BE READY còn lại) | `features/contributor` (DTO đủ 4 ops/Model/Mapper + 12 tests oneOf, fixtures, API fixture 0 request, Query 4 hooks, zod submit/review, 4 components); tab `contributor` ở `/profile` + tab `contrib-apps` dashboard (không đụng auth, không cấp quyền theo requestedType); giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 175/175, `eslint` scope 0 errors, `next build` pass |
| 2026-09-17 | Scaffold Trust-safety leftovers (spec 013-trust-safety-leftovers, T001–T024) | Task #13 mới → 70% (scaffold; test tay TS + BE READY còn lại) | `features/safety` (DTO đủ 2 ops/Model/Mapper + 12 tests UUID-guard, fixtures, API fixture 0 request, Query 2 hooks, zod, nút + 2 dialogs ở shared); gắn báo cáo vào 3 detail + bình luận (chặn tự báo cáo/trùng), thay toast giả xóa lịch sử ở tab privacy; giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 187/187, `eslint` 0 errors, `next build` pass |
| 2026-09-17 | Scaffold Chat sharing/verification (spec 014-chat-sharing-verification, T001–T026) | Task #7 (UC-07) giữ 95% (scaffold; test tay CS + BE READY còn lại) | Mở rộng `features/chat` (DTO suy luận/Model/Mapper + 12 tests, fixtures, API fixture 0 request, Query public không gate auth, zod, 6 components); nút chia sẻ qua slot actions + route `/assistant/public` (link 2 chiều, không AuthGuard) + huy hiệu/nút kiểm chứng theo role thật; giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 200/200, `eslint` 0 errors, `next build` pass |
| 2026-09-17 | Scaffold Notifications (spec 015-notifications, T001–T022) | Task #14 mới → 70% (scaffold; test tay NT + BE READY còn lại) | `features/notification` (DTO suy luận/Model/Mapper + 12 tests timeAgo + capped 9+, fixtures, API fixture 0 request, Query polling 60s dừng tab ẩn + lạc quan rollback, chuông + panel + item ở header thay khối comment-out); giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 212/212, `eslint` 0 errors, `next build` pass |
| 2026-09-17 | Tích hợp Live API Trust-Safety Leftovers (spec 013-trust-safety-leftovers) | Task #13: 70% → 95% | Kết nối live backend `:4000` cho `POST /api/v1/reports` & `DELETE /api/v1/users/me/behavior-history` (tắt fixtures, refine details >= 10 ký tự, hiển thị DeleteHistoryButton tại tab privacy); tsc sạch 0 lỗi, npm test 224/224 pass, next build 30 routes pass |
| 2026-09-17 | Scaffold Restaurants location (spec 016-restaurants-location, T001–T030) | Task #8 (UC-12) 30% → 70% (scaffold; test tay RT + BE READY còn lại) | `features/restaurant` (DTO suy luận/Model/Mapper + 12 tests haversine, fixtures, API fixture 0 request + 0 maps, Query 7 hooks, zod, 10 components); viết lại `/restaurants` + `/restaurants/[id]` (vị trí/form địa chỉ, khung bản đồ CSS cùng tập kết quả) + tab `restaurants` dashboard; giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 224/224, `eslint` 0 errors, `next build` pass |
| 2026-09-17 | Scaffold AI governance (spec 017-ai-governance, T001–T024) | Task #15 mới → 70% (scaffold; test tay AG + BE READY còn lại) | `features/ai-governance` (DTO suy luận/Model/Mapper + 12 tests gồm redaction, fixtures 0 nội dung thô, API fixture 0 request, Query 5 hooks, zod khoảng ngày + lý do, 5 components); tab `ai-governance` dashboard (tổng quan + log che mờ + cờ + công tắc); giữ `PLANNED`/`FE integrated No`; `tsc` sạch, `npm test` 236/236, `eslint` 0 errors, `next build` pass |

