<!-- GENERATED by scripts/sync-swagger.mjs — không sửa tay, chạy lại lệnh để regenerate -->
# Food Data

## GET `/api/v1/food-data/ingredients/{ingredientId}/nutrients`
Lấy nutrient values theo 100 g và provenance của ingredient
- operationId: `getIngredientNutrients`
- Params: `path:ingredientId* (string)`, `query:preparation (string)`, `query:locale (string)`
- Request: —
- Responses: `200` → FoodDataRecordResponse, `400` → ErrorResponse, `404` → ErrorResponse

## GET `/api/v1/food-data/reference-intakes`
Reference intakes và upper limits theo population
- operationId: `listNutrientReferenceIntakes`
- Params: `query:page (integer)`, `query:limit (integer)`, `query:locale (string)`, `query:populationCode (string)`, `query:nutrientCode (string)`, `query:ingredientId (string)`, `query:scope (string)`
- Request: —
- Responses: `200` → FoodDataListResponse, `400` → ErrorResponse

## GET `/api/v1/food-data/ingredient-guidelines`
Ingredient amount/frequency guidelines
- operationId: `listIngredientGuidelines`
- Params: `query:page (integer)`, `query:limit (integer)`, `query:locale (string)`, `query:populationCode (string)`, `query:nutrientCode (string)`, `query:ingredientId (string)`, `query:scope (string)`
- Request: —
- Responses: `200` → FoodDataListResponse, `400` → ErrorResponse

## GET `/api/v1/food-data/cooking-methods`
Cooking methods cùng retention/yield factors
- operationId: `listCookingMethods`
- Params: `query:page (integer)`, `query:limit (integer)`, `query:locale (string)`, `query:populationCode (string)`, `query:nutrientCode (string)`, `query:ingredientId (string)`, `query:scope (string)`
- Request: —
- Responses: `200` → FoodDataListResponse, `400` → ErrorResponse

## GET `/api/v1/food-data/interaction-rules`
Interaction rules theo dish/meal/day
- operationId: `listIngredientInteractionRules`
- Params: `query:page (integer)`, `query:limit (integer)`, `query:locale (string)`, `query:populationCode (string)`, `query:nutrientCode (string)`, `query:ingredientId (string)`, `query:scope (string)`
- Request: —
- Responses: `200` → FoodDataListResponse, `400` → ErrorResponse

---
### Schemas dùng trong nhóm

#### ErrorResponse
```json
{
  "type": "object",
  "required": [
    "success",
    "error"
  ],
  "properties": {
    "success": {
      "type": "boolean",
      "enum": [
        false
      ]
    },
    "error": {
      "type": "object",
      "required": [
        "code",
        "message",
        "requestId"
      ],
      "properties": {
        "code": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "fields": {
          "type": "object"
        },
        "requestId": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
```

#### FoodDataListResponse
```json
{
  "type": "object",
  "required": [
    "success",
    "data",
    "meta"
  ],
  "properties": {
    "success": {
      "type": "boolean",
      "enum": [
        true
      ]
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "meta": {
      "type": "object",
      "required": [
        "page",
        "limit",
        "total",
        "totalPages"
      ],
      "properties": {
        "page": {
          "type": "integer"
        },
        "limit": {
          "type": "integer"
        },
        "total": {
          "type": "integer"
        },
        "totalPages": {
          "type": "integer"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
```

#### FoodDataRecordResponse
```json
{
  "type": "object",
  "required": [
    "success",
    "data",
    "meta"
  ],
  "properties": {
    "success": {
      "type": "boolean",
      "enum": [
        true
      ]
    },
    "data": {
      "type": "object"
    },
    "meta": {
      "type": "null"
    }
  },
  "additionalProperties": false
}
```
