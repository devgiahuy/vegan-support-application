# Backend API Base URL (public, dùng cho axios client + rewrites)
NEXT_PUBLIC_API_URL=

# App Base URL
NEXT_PUBLIC_APP_URL=

# Backend URL server-only (ưu tiên cho serverFetch, không lộ ra browser)
# BACKEND_API_URL=