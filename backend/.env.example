NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://vegan:vegan_local@localhost:5432/vegan_support?schema=public
FRONTEND_ORIGIN=http://localhost:3000
JSON_BODY_LIMIT=1mb
SHUTDOWN_TIMEOUT_MS=10000
LOG_LEVEL=info

# Integration tests must use a dedicated database. When omitted, the test runner
# creates a temporary local PostgreSQL cluster if PostgreSQL binaries are installed.
# TEST_DATABASE_URL=postgresql://vegan:vegan_local@localhost:5432/vegan_support_test?schema=public
